

document.addEventListener("DOMContentLoaded", () => {
   const rosterFile = document.getElementById("roster-file") as HTMLInputElement,
      attendanceData = document.getElementById("attendance-file") as HTMLInputElement,
      outputFName = document.getElementById("outfile-name") as HTMLInputElement;

   console.log(attendanceData);
});
